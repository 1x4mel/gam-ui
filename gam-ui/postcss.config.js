// Tailwind v4 is processed by the @tailwindcss/vite plugin (see vite.config.js).
// No PostCSS plugins are required here; this file is intentionally a no-op
// kept for tooling compatibility.
export default {
  plugins: {},
}
